name: pro_lsp_sdk
description: SDK extension package for pro_lsp providing document synchronization VFS, diagnostic managers, and configuration caches.
publish_to: none
version: 0.1.0

environment:
  sdk: "^3.10.0"

dependencies:
  meta: ^1.9.0
  pro_lsp:
    path: ../pro_lsp

dev_dependencies:
  pro_lints: ^6.1.0
  stream_channel: ^2.1.4
  test: ^1.24.0
